{
  "name": "@kartoffel/monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "npm run dev --workspace=packages/ui-library",
    "dev:app": "npm run dev --workspace=apps/kartoffel-web",
    "build:app": "npm run build --workspace=apps/kartoffel-web",
    "storybook": "npm run storybook --workspace=packages/ui-library",
    "build:lib": "npm run build --workspace=packages/ui-library",
    "test": "npm run test --workspaces --if-present",
    "lint": "eslint .",
    "tsc": "tsc --noEmit",
    "format": "prettier --write \"**/*.{ts,tsx,json,md}\""
  },
  "devDependencies": {
    "@types/node": "^24.12.2",
    "@typescript-eslint/eslint-plugin": "^8.58.1",
    "@typescript-eslint/parser": "^8.58.1",
    "eslint": "^9.18.0",
    "eslint-plugin-react": "^7.37.5",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-storybook": "^10.3.5",
    "prettier": "^3.8.2",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.58.1",
    "vite": "^8.0.8"
  },
  "engines": {
    "node": ">=24.14.1"
  }
}
